name: Compile Demo Page

on:
  push:
    branches:
      - main
    paths:
      - 'docs/**'
      - 'src/**'
      - 'cli.ts'

jobs:
  compile:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install

      - name: Compile docs/main.xiv
        run: ~/.bun/bin/bun run cli.ts docs/main.xiv -o docs/index.html

      - name: Commit and push if changed
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add docs/index.html
          # The following command exits with 0 if there are no changes, and 1 if there are changes.
          # We use || true to prevent the workflow from failing if there are no changes.
          git diff --staged --quiet || git commit -m "docs: auto-compile demo page [skip ci]"
          git push
